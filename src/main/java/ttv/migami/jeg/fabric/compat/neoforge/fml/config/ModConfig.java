package ttv.migami.jeg.fabric.compat.neoforge.fml.config;

public final class ModConfig {
    private ModConfig() {
    }

    public enum Type {
        CLIENT,
        COMMON,
        SERVER
    }
}
