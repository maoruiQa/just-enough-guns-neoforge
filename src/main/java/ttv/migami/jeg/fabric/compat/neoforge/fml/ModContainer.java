package ttv.migami.jeg.fabric.compat.neoforge.fml;

import ttv.migami.jeg.fabric.compat.neoforge.fml.config.ModConfig;

public class ModContainer {
    public void registerConfig(ModConfig.Type type, Object spec) {
    }
}
